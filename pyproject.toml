[project]
name = "HtuiML"
version = "0.0.2"
description = "A tool to convert your basic HTML and very simple CSS into a TUI app!"
requires-python = ">=3.11"
dependencies = [
    "beautifulsoup4",
    "lxml"
]